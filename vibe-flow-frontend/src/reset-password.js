import React from 'react';
import ReactDOM from 'react-dom/client';

import ResetPasswordForm from './reset password/ResetPasswordForm.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ResetPasswordForm />
  </React.StrictMode>
);